import React, { FC } from 'react';
import { View, Text } from 'react-native';

const ChatPage: FC = () => {
  return (
    <View>
      <Text>Chat Page</Text>
    </View>
  );
};

export default ChatPage;
